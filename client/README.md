# Calculator Client

TypeScript frontend for the calculator API.

## Local development

1. Start the API (`Calculator.Api`) on `http://localhost:5000`.
2. Run the frontend dev server.

```bash
npm install
npm run dev
```

The Vite dev server proxies `/api/*` to `http://localhost:5000`.

## Build

```bash
npm run build
npm run preview
```


