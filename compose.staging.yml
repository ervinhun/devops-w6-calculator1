services:
  calculator-api:
	image: ${IMAGE_NAME}
	container_name: calculator_api_staging
	restart: unless-stopped
	ports:
	  - "8080:8080"
	environment:
	  ASPNETCORE_URLS: http://+:8080
	  ConnectionStrings__CalculatorDb: ${CALCULATOR_DB_CONNECTION}

